# Image Resizer

A browser-based image resizer that runs entirely client-side — no upload to a server.

Upload an image, adjust width/height (with an optional aspect-ratio lock and quality slider), preview the result, and download it as a resized JPEG/PNG via the canvas API.

## Run

Open `index.html` in any browser.